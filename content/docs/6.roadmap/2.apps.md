---
title: Apps roadmap
description: Track built-in workspace surfaces without confusing them with integrations or an app marketplace.
navigation:
  icon: i-ph-app-window
---

Discoflare apps are top-level workspace surfaces that share one membership and authorization model. Integrations connect an external capability. These are different product boundaries.

## Available

| App | Released role |
| --- | --- |
| Chat | Channels, Direct Messages, Threads, files, presence, notifications, and optional Huddles. |
| Tasks | Boards and durable Agent work with assignment, dependencies, progress, results, and Run history. |
| Mail | Shared domain Mailboxes, external conversations, Internal Notes, and per-Mailbox access. |
| Databases | Human-managed Databases with typed custom Fields, inline Record editing, filtering, and sorting. |

## Available: Databases

Databases shipped in `v0.1.3` as a human-managed structured workspace surface. Its scope is deliberately smaller than a general database platform:

- workspace Databases;
- typed custom Fields;
- inline Record editing;
- filtering and sorting; and
- permission-gated schema and Record changes.

Databases remain in D1 as workspace state. Discoflare does not provision a separate Cloudflare D1 database for every user-created Database, and Tasks or Mail are not reduced to special cases of this generic record model.

Discoflare imposes no limit on the number of Databases, Fields, or Records. The practical ceiling is the D1 storage and query capacity of the Cloudflare plan the workspace is deployed onto.

## Exploring: Project

A Project surface may connect Task execution to an existing GitHub repository, pull requests, checks, previews, and deployment evidence. The design goal is coordination and visibility; GitHub remains the source-control and review system.

See the [Agents roadmap](/docs/roadmap/agents) for the proposed first repository workflow.

## Planned cross-app work

- clearer links between source conversations, Tasks, Mail conversations, structured Records, and Agent Runs;
- consistent search and activity signals across app boundaries; and
- evidence views that distinguish saved workspace state, external-provider state, and deployed runtime state.

## Not available today

Discoflare does not currently expose an installable app marketplace, arbitrary third-party app runtime, or compatibility promise for draft internal APIs. New extension points need a permission, data, audit, and upgrade model before they can become a public platform surface.
